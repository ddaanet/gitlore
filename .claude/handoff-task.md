## Current task

Nothing in flight. The release/memory push race is settled as D46 — a push refused by divergence is resolved and pushed again, the parent commit is never amended to re-pin memory — with the argument in `docs/references/git-hooks-and-entry-points.md`, NFR9/NFR10 detail moved to the new `testing.md` node to make hub room, and the plugin-dev side filed as a brief in `../claude-plugin-dev` (end of involvement there). What remains are the two decisions in the todo, neither started.
