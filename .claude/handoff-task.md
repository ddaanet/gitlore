## Current task

Reviewing all 100 `memory/ddaanet/` facts against the `memory-writing` rubric,
ordered by index-line size largest first, one memory at a time with my human
partner validating each verdict before the next. The ledger spans
`plans/ddaanet-memory-review.md` — the hub holding the verdict table and
entries 1, 1b, 3, 4 and 4b — plus parts 2a-2d carrying entry 2. Entry 1 is
settled as keep-as-written; entries 2, 3 and 4 all carry proposed changes my
human partner has not yet ruled on.

Entry 4b turned the pass. Rather than editing `memory-writing` and
`index-compaction-triggers` in place, both are proposed for conversion into
gitlore skills — which would clear the loader-cap overflow that motivated the
review, and settle the deferred guide-shaped-fact question for
`green-is-not-evidence` and `design-doc-writing` in the same move.

Edits to the memory files themselves stay deliberately deferred to the end of
the pass, so the index and the frontmatter descriptions are rewritten once
rather than per entry.