## Current task

Orchestrating `plans/tier-arrival-review-minors` with `/edify:orchestrate`. Phases 1–4 are implemented, reviewed per slice and checkpointed, each with a green `just precommit`; Phase 5 (test specificity, Items 5.1–5.4, type general) opens next, then Phase 6 (prose) and Phase 7 (docs), then the final review, the TDD audit and the closing run summary.

Two Phase 4 gaps are approved and come before Phase 5, both found by `plans/tier-arrival-review-minors/reports/phase-4-corrector.md`, which enumerates every pre-landing exit of `continue-after-merge`. First: a failed `mktemp` for the merge message file exits 1 with mktemp's own text and no `gitlore:` line, one line above the two arms Item 4.1 just gave lines to; it becomes a third arm of Item 4.1 with its own test, which needs a runbook list revision. Second: Phase 6's merger prose becomes an allow-list — only a recognised landed line means landed — so that gap, the two `load_continuation_state` refusals that speak outside the key, and a malformed state file are all safe without new messages.

The per-slice reports under `plans/tier-arrival-review-minors/reports/` are the record the closing summary and the TDD audit read: every Phase 1–4 slice has RED, test review, GREEN and code review except where a slice was a guard (1.1/5, 2.2/2, 3.1/2 — each proven by mutation, each with a test review) or born green. Slice 2.1/1's code review saw a shape slice 2.1/2 reverted, so Item 2.1 was re-reviewed whole.
