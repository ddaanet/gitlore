## Current task

Release 0.2.2 (patch): 13 unpushed commits on main since v0.2.1 — linked-worktree install fix, evals infrastructure, sandbox pre-flight; `make test` passes 9/9; run `just release patch --yes`.
