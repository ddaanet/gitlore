## Current task

Acting on `inbox/brief-index-edit-propagation-findings.md`: triage, root-cause
and outline are written under `plans/index-edit-propagation/`, and `/proof` on
`outline.md` is open at its orientation checkpoint with no items reviewed yet.
Item A of the outline (the compose `additionalContext` sentence) is already
applied; B and C are unwritten.

Running alongside it, still: the ddaanet memory curation — the index budget
against the loader cutoff, the design-moment tier merges, the oversized-fact
splits, and the review pass.
