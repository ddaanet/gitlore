## Current task

The 21 Minor findings of the unadoptable-tier-arrival deliverable review are designed and planned as job `plans/tier-arrival-review-minors`: outline proofed, runbook written, corrected by runbook-corrector (reports/runbook-review.md), consolidated by runbook-simplifier (reports/runbook-simplify.md), and passed proof with every item skipped. Next is `/orchestrate plans/tier-arrival-review-minors` in a fresh session. Phase 2 opens on a reproduction gate: if neither race variant goes red against the current code, the phase stops and reports. The runbook is 472 lines, over the 400-line soft cap, left as one cohesive plan. The review's Major (merge skill's unconditional `/gitlore:push` claim) stays in the todo list, outside this job.