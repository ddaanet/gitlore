## Current task

Reviewing all 100 `memory/ddaanet/` facts against the `memory-writing` rubric,
ordered by index-line size largest first, one memory at a time with my human
partner ruling on each verdict before the next. The ledger spans
`plans/ddaanet-memory-review.md` — the hub holding the verdict table and
entries 1, 1b, 3, 4, 4b and 4c — plus parts 2a-2d carrying entry 2.

Entries 1 and 4 are settled at verdict **plugin**: `memory-writing` becomes a
`gitlore:memory-writing` skill, and `index-compaction-triggers` dissolves into
that skill plus a manual `/gitlore:index-audit` command. Entry 4c holds the
shape — which handoff surface hosts the pointer, why the write-time hook is
held back, and what condensation cuts. Entries 2 and 3 still carry proposals
with no ruling.

Memory-file edits stay deferred to the end of the pass, so the index and the
frontmatter descriptions are rewritten once rather than per entry.
