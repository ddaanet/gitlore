## Current task

Slice 3a (structural index recompose — `scripts/lib/index-recompose.sh`, SessionStart-wired, 270 unit tests green) is done; the next session opens with an **eyeball review of this commit** (David's explicit request) before starting slice 3b (first nested tier).

## Open decisions

- Slice 3b needs its own fresh plan (plan-as-late-as-possible) — scope it from D17's deferred items: nested-tier submodule materialization, tier-block placement + carrier mirroring, routing guidance, recursive scan that won't prune unchecked-out tier lines.
