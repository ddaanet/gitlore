## Current task

The 400-line split run is finished: nineteen pure-move splits, each reviewed, with the per-split reports and the shared agent brief in `plans/file-splits/`. Next is fixing the two gate defects the run surfaced — the first two items of the todo list: `scripts/lint-shell.sh` skipping untracked files, and `tests/justfile_gates.bats` overwriting the real `test-integration` sentinel.
