Fix `/gitlore:push` misreading a *behind* store as diverged, dying at `scripts/lib/resolve.sh`. Brief at `plans/brief-push-misreads-behind-as-diverged.md`, filed from a micro session against gitlore 0.4.5 — the file is byte-identical in 0.5.0, so the defect is in the shipped release.