## Current task

The deliverable review of `plans/unadoptable-tier-arrival` (range `e60ff38..HEAD`) is written: `plans/unadoptable-tier-arrival/reports/deliverable-review.md` consolidates the three opus layer-1 reports beside it (`deliverable-review-{code,test,prose}.md`), with 0 Critical, 1 Major, 21 Minor. Next is `/edify:design` over the 21 Minors, as one job: messages and error signaling in `gitlore_adopt_repair_arrival` and its walk-back (unrepairable arm omits non-carrier problems, transient arms say "Fix the store", walk-back says `live` keeps what arrived when it holds the repair), the `behind` arm retry push's divergence wording, the unterminated-last-duplicate newline loss in `gitlore_repair_index` (probed), the scratch directory a killed take leaves, the index-compose comment verb and the `(K3)` test header, four test-specificity gaps, and eight prose/doc wording fixes. The Major (merge skill's unconditional `/gitlore:push` claim) is tracked in the todo list, not in this design.
