## Current task

The `docs/` graph is capped at 400 lines per file, enforced by the new `oversized-file` check in `scripts/check-docs-links.py`; the split landed (nine new nodes, hub at 393 with its conclusion bullets and per-group *Rejected* lines) and the gates are green. What resumes is the checker follow-up: `check_orphans` searches for the literal `references/<basename>.md`, so a node linked only by a sibling-relative link (`[x.md](x.md)`, the form the nodes use among themselves) reads as an orphan — the three evals nodes clear the warning today only because the 2026-08-25 changelog entry names them by full path.
