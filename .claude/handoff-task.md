## Current task

Nothing in flight — the intermittent `make_parent_with_memory` failure in the
integration suite is diagnosed, fixed and pinned by a new unit suite. One thread
stands open for a later session: the ddaanet memory review pass, which resumes at
entry 5 of `plans/ddaanet-memory-review.md`.
