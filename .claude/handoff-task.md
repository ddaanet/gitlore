## Current task

Acting on `inbox/brief-index-edit-propagation-findings.md`: triage, root-cause,
probe and outline are under `plans/index-edit-propagation/`, and `/proof` on
`outline.md` is complete — all ten items carry a verdict and the batch is
applied. The outline now scopes four changes: A (the compose `additionalContext`
sentence) is applied; B (the commit path composes before it commits), C
(pre-image and compose-stamp paths keyed per `agent_id`) and D (relay a
subagent's compose and index-sync reports to the parent) are unwritten. D
depends on C. `/runbook` is the next pipeline step.

Running alongside it, still: the ddaanet memory curation — the index budget
against the loader cutoff, the design-moment tier merges, the oversized-fact
splits, and the review pass.
