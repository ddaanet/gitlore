## Current task

Reviewing all 100 `memory/ddaanet/` facts against the `memory-writing` rubric,
ordered by index-line size largest first, one memory at a time with my human
partner ruling on each verdict before the next. The ledger spans
`plans/ddaanet-memory-review.md` — the hub holding the verdict table and
entries 1, 1b, 3, 4, 4b and 4c — plus parts 2a-2d.

Entries 1 through 4 are settled. `memory-writing` and `index-compaction-triggers`
convert to a `gitlore:memory-writing` skill plus a manual `/gitlore:index-audit`
command, with the shape in 4c. Entry 3's relink and its redrafted 690 B index
line are approved and drafted in the ledger; entry 2's changes (a) through (f)
are approved, two of them refined during the ruling. Entry 5,
`hook-input-schema`, was opened and its body read, but no rubric was run and no
verdict was produced.

Memory-file edits stay batched to the end of the pass, so the index and the
frontmatter descriptions are rewritten once rather than per entry.
