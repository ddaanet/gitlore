## Current task

Nothing in flight. Ledger 4c of the ddaanet memory review is shipped: the `memory-writing` skill (with `no-speculative-rules` folded in), the `/gitlore:index-audit` command, D47 in `index-authoring-sync.md`, and the three fact files retired. The review pass resumes at entry 5.
