## Current task

A run over the handoff's Remaining list, one opus sub-agent per batch, each gated by `just precommit` before the next started, worked through the items that were specified well enough to implement; the per-batch reports under `plans/2026-09-18-remaining-items/reports/` carry the evidence and the judgement calls, and `docs/changelog/2026-09-19-a-tier-ahead-of-its-pin-returns-to-it-and-a-stray-line-reaches-the-check.md` records the behaviour changes. What is left is the items that need a ruling, a design, another machine or elapsed time, plus the file splits. The `/edify:orchestrate` run of `plans/tier-arrival-review-minors` still awaits `/deliverable-review plans/tier-arrival-review-minors` (opus, fresh session).
