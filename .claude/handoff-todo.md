## Remaining

- Run `/deliverable-review plans/tier-arrival-review-minors` (opus, fresh session).
- Fix deliverable-review Major 1 of `plans/unadoptable-tier-arrival`: `skills/merge/SKILL.md:68-74` tells the agent `/gitlore:push` publishes a repair unconditionally, but a repair resting on this repo's own index problems publishes nothing. Make the claim conditional on the run printing `…; /gitlore:push publishes it.`, and in the resting case say it publishes once the listed problems are fixed. The same passage says an unrepairable arrival "must be fixed where it was published" without the two-fix remedy (`Fix the problems listed above in this repo; once the index is fixed where it was published, run /gitlore:merge again.`), and omits the transient-failure remedy `Run /gitlore:merge again.`
- Move `memory/ddaanet/gitlore-tier-merge-direction.md` into the plugin, in the same pass as the Major 1 fix above (same file; prose, so opus): the mechanism — projection direction, and root at `HEAD` deciding a carrier-only path — into the index-authoring node under `docs/references/`, the procedure — check `git show HEAD:memory/MEMORY.md` before assuming one pass suffices, land upstream first, correct as a follow-up change — into `skills/merge/SKILL.md`. Retire the tier fact once it has shipped.
- Pin the behind arm's retry push routing: `gitlore_push_stores`'s behind arm sends its in-arm retry push through `gitlore_report_tier_push_failure`, so a non-fast-forward refusal of that retry gets the divergence wording, and no test exercises it. A stubbed second `push -q origin live` in the behind-arm fixture of `tests/push_behind_vs_diverged.bats` pins it.
- The entry-wise index pass (`scripts/lib/index-merge.sh`, `_gitlore_index_merge_bullets`) drops interleaved non-bullet and blank lines inside the pointer region with rc 0 in ordinary merges, and `gitlore_merge_indexes` stages the result. Consequence: rule 4 of `gitlore_compose_check_index` never fires at the merged-root gate in an ordinary root merge — the stray line is gone before the check runs; `tests/resolve_compose.bats`'s interleaved-line test reaches rule 4 only by adding a duplicate path that makes `gitlore_index_merge` return 2. Schedule it as its own defect.
- Write a mutation helper, e.g. `scripts/mutate-and-run.sh <file> <patch> <bats-file> <filter>`, that refuses when the target has uncommitted edits or the patch applies empty, keeps its backup under `.git/`, restores, and verifies `git diff` is empty. Five reviews in one run mutated with `$TMPDIR` unset; one backup went to `/`.
- Split against the 400-line cap: `scripts/lib/index-compose.sh` (~1075 lines) and `scripts/lib/resolve.sh` (~2170 lines); and the eleven bats suites past 400, largest first — `index_sync` (1710), `index_compose` (1425), `merge_memory` (1231), `commit_memory` (967), `resolve_compose` (802), `push_behind_vs_diverged` (749), `resolve_recovery` (699), `cc_hook_index_compose` (597), `git_hook_pre_commit` (509), `cc_hook_session_start` (506), `add_tier` (495). Split along each file's section banners, moving shared setup into `tests/helpers/`; `tests/push_behind_vs_diverged.bats`' publication sections go to `tests/push_tier_publication.bats`.
- Close the batch-retry approval gap: on the PostToolBatch path the retry reuses the preserved summary, so the edit fixing an aborting index commits under the old approval although the abort text says the summary needs approval again; needs a status protocol between `commit-memory.sh` and the batch hook.
- A take killed after `gitlore_compose_up` writes root but before the pair is staged leaves root describing a tier SessionStart pins back; the next compose projects it down and dirties the tier.
- Confirm a repair take end to end on macOS (bash 3.2); nothing on this box exercises the bash < 4.4 hazards.
- Add the untested review-fix behaviours from `plans/unadoptable-tier-arrival/reports/tdd-audit.md` F2: a failed `git status` in the commit path's rc 1 arm restamps and aborts; `gitlore_repair_index` keeps the file mode; `hash-object --no-filters` keeps CRLF bytes. Add a refused-`push .` mutation red for Item 2.2 slice 6.
- `resolve.sh`'s "remote has no live branch. Pushing." step pushes memory's `live` before any tier gate on a never-published memory remote; unprobed.
- `_gitlore_nudge_reset`'s `find … -mtime +7 -delete` is unguarded under the calling hook's errexit; `gitlore_relay_sweep` guards its own.
- `plans/index-edit-propagation/recall-artifact.md` names four memory files that no longer exist under `memory/ddaanet/`: `hook-input-schema`, `genuine-red-not-missing-sut`, `green-is-not-evidence`, `design-doc-writing`. Find where they went and repoint or retire the artifact lines.
- Check whether a tier whose local `live` failed to advance after its commit, for a non-divergence reason, ever advances. `gitlore_sync_tiers_to_live` pushes `HEAD:live` only for dirty tiers, so the retry that stages the landed commit skips the push.
- `record-sentinel` in the justfile prolog hashes the gate inputs after the ~9-minute run, so a peer session's edit during the run records a pass for a tree nothing checked — the likely cause of `precommit` verdicts spanning two trees (not reproduced). Hash at guard time, re-hash at record time, record only on a match, otherwise say the pass was not recorded. Pin with a test that edits an input mid-run.
- Pin guard, ahead-of-pin branch of `gitlore_compose_check_pins`: when the tier is clean and its local `live` contains `HEAD`, check the tier out at its pin and direct to `/gitlore:merge`, which adopts through `gitlore_adopt_advanced_live`; otherwise keep the refusal. Reword the remaining "no automatic remedy" text, which prescribes rebuilding root's tier block by hand — what `gitlore-tier-merge-direction` forbids. Record under D50.
- Persist the merged-index gate's problem lines in the merge state file and have `gitlore_emit_merge_directive` emit them, so a directive re-emitted after a clear or compact carries them; then cut `skills/resolve/SKILL.md`'s resume-the-same-sub-agent workaround down to the stop rule. No retry cap.
- A memory store with no root `MEMORY.md` runs no index check, as `tier-arrival-repair.md` states, and says nothing about it. Emit a notice when root is absent (index checks off, store cannot be merged into), and check whether install's copy-existing-auto-memory branch can produce a rootless store.
- Make `scripts/cc-hooks/relay-drain.sh` and `scripts/cc-hooks/index-compose.sh` omit `additionalContext` when the ctx half is empty, using the conditional `jq` form in `scripts/cc-hooks/index-sync-post.sh`.
- Soften the index-budget reporting wording (`scripts/cc-hooks/index-sync-post.sh` and wherever else the percentage is reported): the budget is an ambient notification, not a goal to achieve.
- Add to `.claude/rules/shell.md`: **`set -e` does not abort a Bash tool command.** A `cd "$TMPDIR"` with `$TMPDIR` unset leaves the shell in the repo root and the following commands run there. The unset-`$TMPDIR` half is already recorded; the non-aborting half is not.
- Narrow `test-unit`'s gate inputs to exclude `tests/integration_*`.
- Rewrite `CLAUDE.md` §Testing's gate paragraph: a sentinel is valid when its recorded content hash of the gated inputs matches, not by mtime; the sequential fallback includes `just check-distribution`; `format-docs`, `check-memory-hygiene.py`, `check-docs-links.py` and `check-version` run uncached and write no gate file, so the gates cannot vouch for them; the gates path is per-worktree; drop the "box will not take two suites" rationale — the kills were Claude Code's background-shell pressure reap, off via `CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1` in `.claude/settings.local.json`.
- Exclude `plans/*/reports/` from `just format-docs`' wrap set (recommendation B of `plans/2026-09-10-orchestrate-context-analysis.md`). First confirm the re-injections are rewrap-driven — `git log -p --stat` on one report against the session's precommit times — and check the exclusion against the `oversized-file` checker's line counts.
- Ask, then drop one brief in edify, which ends gitlore's involvement: recommendations A, C, D and E of `plans/2026-09-10-orchestrate-context-analysis.md` (A is a `skills/orchestrate/SKILL.md` edit too), and the process recommendations of `plans/index-edit-propagation/reports/tdd-audit.md` and `plans/tier-arrival-review-minors/reports/tdd-audit.md` — a gate verdict in each GREEN report, every bats file that sources the changed script listed, a guard's mutation proof in an independent test review, no skipped reviews for a small item, one slice per commit, and a committed `reports/phase-N-gate.md` holding the `just precommit` recap line and tip sha.
- Ask, then drop one note each in shell-scripting, plugin-craft and craft from `plans/2026-09-18-memory-pass-relocations.md`, and fold its edify section into the edify brief above; each note ends gitlore's involvement. The pass saved none of the eighteen facts as memory, so their substance lives only in that file.
- Record in `docs/decisions.md`: the stale-merge guard's report-only arms do not restamp the approval (rejected: a per-arm return code from `gitlore_guard_stale_merge_state` — a misclassified arm would silently approve content no summary covered, against one visible re-approval saved); and a failure-time restamp blessing a concurrent write is an accepted residual (rejected: a `touch -r` snapshot, since the success path has the same window up to `add -A`). Add the residual sentence to `docs/references/git-hooks.md`.
- State once, in `tests/bsd_portability.bats`' header or the testing notes, that the suite's own assertions assume bash ≥ 4.1 — a failing non-final `[[ ]]` does not fail a test under bash 3.2 — so a macOS run uses a modern bash for bats. No `|| return 1` sweep and no per-touch style rule.
- Triage `inbox/brief-add-tier-index-budget-advisory.md`, `inbox/brief-index-audit-preamble-and-merge-unlanded.md`, `inbox/brief-session-start-protocol-context-wording.md` and `plans/brief-codex-native-memory-integration.md`. `inbox/brief-conventions-file-oversize-false-positive.md` is settled: the "over the 4KB recall limit … rewrite it to under 2.8KB" error is Claude Code's own (its text is in the 2.1.276 binary and nowhere in gitlore or a ddaanet plugin), so gitlore cannot exempt the file, and the conventions file stays inside the memory directory because it travels as a tier. The remedy is the exemption line below.
- Split the oversized token-keyed facts so recall reaches them: `hook-output-channels` (23% reachable), `bats-shellcheck-gotchas` (40%), `stale-plugin-code` (45%), `design-doc-writing` (over 4KB).
- Extend the memory-writing skill's index-line guidance to both axes.
- Rerun `plans/2026-09-02-recall-log-analysis.py` once a week or two of native-recall attachments exist.
- Continue the ddaanet review pass from `plans/ddaanet-memory-review.md` (entry 5, `hook-output-channels`).
