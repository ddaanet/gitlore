## Open decisions

- Where the subagent full-suite rule lives: a dispatch stops at targeted suites plus `just lint` and the main session runs the full gate in the background, because a subagent's full `just test-unit` outlives the Bash tool's 10-minute foreground wait, is moved to the background and stalls there (`plans/file-splits/brief-bats.md` step 6 carries the wording). Proposed: one clause amending the shared-claude Dispatch bullet that says to run the suite in the foreground; the project `CLAUDE.md` Testing section is the narrower alternative. The `.md` report refusal seen once from a general-purpose subagent did not recur across four dispatches writing `.txt` reports under `/tmp/claude/gitlore-reports/`; proposed: write nothing unless it recurs.
- Whether `check_store_gates` in `scripts/resolve.sh` guards its `fetch -q origin live || true`: on a first publish it leaks `fatal: couldn't find remote ref live` to the user. Fetching only when `ls-remote origin live` is non-empty removes the line and costs one network round trip per store on every resolve run.
- How to split the files still over 400 lines. Proposed: leave `scripts/check-memory-hygiene.py` (405) as a bounded overage; extract the decisions-index checks (`collect_*`, `check_coverage`, `check_citations`, `check_enumeration`, about 200 lines) from `scripts/check-docs-links.py` (577) into a sibling module, settling where it lives, whether the two checkers share `git_toplevel`/`read_text`/`report`, and whether `check-distribution` and the gate input lists pick it up; for `scripts/cc-hooks/session-start.sh` (429), choose which stages of its sequential flow move into `scripts/lib/`.

## Remaining

- Read `plans/macos-check/out/report.txt` once my human partner has run `plans/macos-check/run.sh` on a Mac: the assertion probe settles the claim in `tests/bsd_portability.bats`' header (a failing non-final `[[ ]]` does not fail a bats test under bash 3.2), the `mixed` run is the verdict on a repair take under 3.2, and `all-old` plus the cached-runner probe cover `tests/helpers/fixtures.bash`'s `${s//…/…}` escapes and `scripts/run-bats-cached.sh`. Correct the header and `docs/references/testing.md` if the probe refutes the claim.
- Close the batch-retry approval gap: on the PostToolBatch path the retry reuses the preserved summary, so the edit fixing an aborting index commits under the old approval although the abort text says the summary needs approval again; needs a status protocol between `commit-memory.sh` and the batch hook, designed before it is coded.
- Triage `inbox/brief-add-tier-index-budget-advisory.md`, `inbox/brief-index-audit-preamble-and-merge-unlanded.md`, `inbox/brief-session-start-protocol-context-wording.md` and `plans/brief-codex-native-memory-integration.md`, once the rest of the list is cleared; each brief's recommendation is input, and the choice is my human partner's.
- Split the oversized token-keyed facts so recall reaches them: `hook-output-channels` (23% reachable), `bats-shellcheck-gotchas` (40%), `stale-plugin-code` (45%), `design-doc-writing` (over 4KB).
- Rerun `plans/2026-09-02-recall-log-analysis.py` once a week or two of native-recall attachments exist.
- Continue the ddaanet review pass from `plans/ddaanet-memory-review.md` (entry 5, `hook-output-channels`).
